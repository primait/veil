name: CD

on:
  release:
    types: [published]

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: install cargo-release
        uses: taiki-e/install-action@ace6ebe54a6a0c86dfb5f7764b17f793b6925bc3 # ratchet:taiki-e/install-action@v2
        with:
          tool: cargo-release@0.24
      - name: login
        run: cargo login "$CARGO_AUTH_KEY"
        env:
          CARGO_AUTH_KEY: ${{ secrets.CARGO_AUTH_KEY }}
      - name: publish
        run: cargo release publish --no-confirm --allow-branch "*" --workspace --all-features --execute
